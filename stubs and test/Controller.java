import javafx.scene.Scene;
import javafx.stage.Stage;

public class Controller {

	private Model model = new Model();
	private View view = new View();
	
	/* The start method is called when the JavaFX application is ran. Opens and runs the JavaFX application.
	 * @param stage - the stage where all the visual parts of the JavaFX application are displayed
	 * @return void
	 */
	@Override
    public void start(Stage stage) {
    	
    }
	 
	
}
