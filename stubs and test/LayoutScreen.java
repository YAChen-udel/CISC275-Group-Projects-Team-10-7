import javafx.event.EventHandler;
import javafx.scene.image.Image;
import javafx.scene.layout.StackPane;

public class LayoutScreen extends View{

	/* The loadImage method is to be used to add plant images into the garden.
	 * @param plant - the image to be loaded
	 * @param spane - the StackPane that the image will be added to
	 * @return void
	 */
	public void loadImage(Image plant, StackPane spane) {
		
	}
	
	/* The loadHouse method is to be used to generate the house in the garden view.
	 * @param house - the house to be loaded
	 * @param spane - the StackPane that the house will be added to
	 * @return void
	 */
	public void loadHouse(Image house, StackPane spane) {
		
	}

	/* The hoverOverObject method will be used to handle user interactions of 
	 * hovering over plant images.
	 * @param house - the plant the user is hovering
	 * @return void
	 */
	public void hoverOverObject(Image plant) {
	
	}

	
}
